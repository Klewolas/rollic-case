using RollicCase.UI.Buttons;

namespace RollicCase.UI.Popups
{
    /// <summary>Asks to close the topmost popup.</summary>
    public sealed class ClosePopupButton : SignalButton<ClosePopupRequestedSignal>
    {
    }
}
