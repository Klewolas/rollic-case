namespace RollicCase.Editor.LevelEditor.Board
{
    /// <summary>What a pointer position on the editor board points at.</summary>
    public enum BoardHitKind
    {
        None,
        Cell,
        Edge
    }
}
